body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-color: #121212;
    margin: 0;
    font-family: Arial, sans-serif;
}
.calculator {
    background-color: #1e1e1e;
    padding: 20px;
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.5);
}
#display {
    width: 260px;
    height: 50px;
    background-color: #2a2a2a;
    border: none;
    color: #fff;
    font-size: 24px;
    text-align: right;
    padding: 5px 10px;
    margin-bottom: 15px;
    border-radius: 6px;
}
.buttons {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
}
button {
    padding: 20px;
    font-size: 18px;
    cursor: pointer;
    border: none;
    border-radius: 6px;
    background-color: #3a3a3a;
    color: white;
}
button:hover { background-color: #4a4a4a; }
.operator { background-color: #ff9f0a; color: white; }
.operator:hover { background-color: #cc7f08; }
.equal { background-color: #30d158; grid-row: span 2; height: 100%; }
.equal:hover { background-color: #24a143; }
